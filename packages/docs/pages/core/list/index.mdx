import Preview from '@/helper/preview.vue'
import PreviewFrame from '@/helper/preview-frame.vue'
import CodeHighlight from '@/helper/code-highlight.vue'
import ListExample from './list-example.vue'

export const meta = {
  title: 'List',
  description: 'A list component',
  source: 'packages/lib/src/components/List',
  inherits: []
}

> [!WARNING]
> This component is still in prototype stage.
> Documentation for this element is not yet complete.
> Please do not use it in production.

## Usage

<Preview>
  <PreviewFrame height="300">
    <ListExample />
  </PreviewFrame>
  <CodeHighlight>
    ```vue
    <script setup lang="ts">
      import { List, Box } from '@vue-material/core'
      import { ref } from 'vue'

      type Item = {
        value: number
        label: string
      }

      const defaultItems: Item[] = [
        {
          value: 1,
          label: 'Item 1'
        },
        {
          value: 2,
          label: 'Item 2'
        },
        {
          value: 3,
          label: 'Item 3'
        }
      ]

      const items = ref(defaultItems)
    </script>

    <template>
      <Flex direction="column" align="center" p="#md" width="100%">
        <List v-model="items" sortable swipe="dismiss" />
        <Button @click="items = defaultItems" label="Reset" />
      </Flex>
    </template>
    ```
  </CodeHighlight>
</Preview>
