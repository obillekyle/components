import Preview from '@/helper/preview.vue'
import PreviewFrame from '@/helper/preview-frame.vue'
import CodeHighlight from '@/helper/code-highlight.vue'
import Snackbar from '@vue-material/core/Snackbar'
import Button from '@vue-material/core/Button'
import Flex from '@vue-material/core/Box/Flex'
import { ref } from 'vue'

export const meta = {
  title: 'Snackbar',
  description: 'Snackbars provide brief messages about app processes at the bottom of the screen.',
  source: 'packages/lib/src/components/Snackbar',
  inherits: []
}

## Usage

<Preview>
  <PreviewFrame height="150">
    <Flex align="end" justify="center" style={{height: '100%', paddingBottom: '20px'}}>
        <Snackbar
          class="static"
          message="This is a snackbar"
          actions={[{ label: 'Action' }]}
        />
    </Flex>
  </PreviewFrame>

  <CodeHighlight>
    ```vue
    <script setup lang="ts">
      import { SnackbarManager, Button } from '@vue-material/core'

      function showSnackbar() {
        SnackbarManager.open({
          message: 'This is a snackbar',
          actions: [
            { label: 'Undo', onClick: (utils) => utils.close() }
          ]
        })
      }
    </script>

    <template>
      <Button @click="showSnackbar">Show Snackbar</Button>
    </template>
    ```

  </CodeHighlight>
</Preview>

> **Note:** Requires `<Snackbar.Provider />` in the app root for `SnackbarManager` usage.

## Props

`message?`: string | Component

- Text message to display.

`actions?`: { label: string, onClick: (utils) => void }[]

- Action buttons.

`timeout?`: number

- Duration in milliseconds before auto-closing.

`closeable?`: boolean

- Whether to show a close icon.
