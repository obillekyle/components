import Preview from '@/helper/preview.vue'
import PreviewFrame from '@/helper/preview-frame.vue'
import CodeHighlight from '@/helper/code-highlight.vue'

export const meta = {
  title: 'Number',
  description: 'Number utilities',
  source: 'packages/lib/src/utils/number.ts',
  inherits: []
}

## Imports

<Preview>
  <CodeHighlight>
    ```ts
      import { <fn> } from '@vue-material/core'
      import { <fn> } from '@vue-material/core/utils'
      import { <fn> } from '@vue-material/core/utils/number'
    ``` 
  </CodeHighlight>
</Preview>

## Methods and Properties

### getUnique()

> [!WARNING]
> This method is unreliable for SSR and SSG environments. 
> Please use [`hashStr()`](/utils/string#hashstr) instead.

<Preview>
  <CodeHighlight>
    ```ts
    function getUnique(): number;
    function getUnique(prefix?: string): string;
    ```
  </CodeHighlight>
</Preview>

**`getUnique()`**: string | number
- Get a continuous incremental unique number
- Parameters
  - `prefix?`: string
    - Default: `undefined`

---

### rand()

<Preview>
  <CodeHighlight>
    ```ts
    function rand(min: number, max: number): number;
    ```
  </CodeHighlight>
</Preview>

**`rand()`**: number
- Returns a pseudorandom number
- Parameters
  - `min`: number
  - `max`: number
  - `excludeLimit?`: boolean
    - Default: `false`

---

### mapNumberToRange()

<Preview>
  <CodeHighlight>
    ```ts
    function mapNumberToRange(
      number: number,
      oldMin: number,
      oldMax: number,
      newMin: number,
      newMax: number
    ): number;
    ```
  </CodeHighlight>
</Preview>

**`mapNumberToRange()`**: number
- Maps a number from one range to another
- Parameters
  - `number`: number
  - `oldMin`: number
  - `oldMax`: number
  - `newMin`: number
  - `newMax`: number

---

### findNearestNumber()

<Preview>
  <CodeHighlight>
    ```ts
    function findNearestNumber(number: number, numbers: number[]): number;
    ```
  </CodeHighlight>
</Preview>

**`findNearestNumber()`**: number
- Finds the nearest number in an array
- Parameters
  - `number`: number
  - `numbers`: number[]

---

### clamp()

<Preview>
  <CodeHighlight>
    ```ts
    function clamp(number: number, min: number, max: number): number;
    ```
  </CodeHighlight>
</Preview>

**`clamp()`**: number
- Clamps a number between two values
- Parameters
  - `number`: number
  - `min`: number
  - `max`: number

---

### toFileSize()

<Preview>
  <CodeHighlight>
    ```ts
    function toFileSize(value: number, type: 'bit' | 'byte'): string;

    toFileSize.bits = (value: number) => toFileSize(value, 'bit')
    toFileSize.bytes = (value: number) => toFileSize(value, 'byte')
    ```
  </CodeHighlight>
</Preview>

**`toFileSize()`**: string
- Convert a number to a file size
- Parameters
  - `value`: number
  - `type`: 'bit' | 'byte'

